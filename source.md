---
title: "模型学会流程之后，Skill如何自进化？"
date: 2026-09-07
description: "模型逐步学会通用工作方法后，旧Skill里的步骤还剩多少价值？从三组实测结果出发，讨论流程精简、必要约束和持续迭代。"
draft: true
slug: "skill-self-evolution"
---

一套Skill里的流程，往往是为了解决模型当时做不好的事。模型能写代码，却可能漏掉需求、跳过验证，或在多轮修改中丢失前面的结论。于是，工程师把经验整理成模型可以按需读取的工作指南，规定分析和验证的顺序，必要时再配上工具脚本。这些指导有助于减少遗漏，让模型更稳定地完成任务。

这套安排有一个前提：模型仍然需要这样的帮助。随着后训练强化推理、自我检查和策略调整，部分通用方法逐渐进入模型能力。过去需要逐步提醒的工作，模型现在可能已经会主动完成。**原先为弥补模型能力不足而增加的步骤，也可以相应精简。**

如果Skill没有跟着调整，旧步骤仍会照常执行。模型即使已经掌握任务信息，也可能被要求重新整理计划、生成报告，再按固定轮次分析。重复的不只是几段说明文字，还包括它们触发的读写、工具调用和后续输入。原来用于减少遗漏的流程，就可能开始增加不必要的开销。

比如，模型已经能自行分析代码，Skill还要求它按固定模板写几轮分析报告。省去这些重复报告后，模型仍然要读取当前代码、运行测试，才能确认这次修改是否符合预期。随着模型能力的提高，Skill可以少规定一些分析步骤，重点说明当前项目有哪些要求、交付前必须验证什么。

![模型逐步内化通用方法，Skill继续提供项目事实、配套脚本和交付约束。](assets/01-model-skill-boundary.png)

*图1：模型能力变化后，重新检查Skill还需要补充什么。*

## 实验背景与研究问题

围绕单元测试任务，生成、分析和优化三个Skill分别负责测试生成、有效性分析和迭代优化，并结合编译运行检查结果。实际项目的运行记录显示，模型在完成任务的过程中做了不少重复工作：同一份源码被多次读取，中间报告反复写入，有些验证轮次没有带来有效变化。这些操作增加了开销，后续优化便从减少这部分重复工作入手。

后续版本删减了通用流程说明和中间报告，并将源码与环境信息读取、编译运行等操作整合为配套脚本，减少模型反复查找入口和组织命令的工作。同时，根据精简后的失败反馈，补充必要的交付要求，避免减少流程影响结果质量。

这些要求写进Skill后，后续任务就会按修改后的规则执行。人工与模型再结合新的运行记录，比较新旧版本的质量和开销，决定哪些改动值得保留、哪些还要继续调整。这样，Skill就能根据实际运行中发现的问题持续改进。

评测保留了使用完整流程的旧版Skill，与调整后的新版比较质量和开销。同时设置无Skill对照组，检验模型能否自行完成简单任务。三组各覆盖7个项目、140个对应任务。

三组实验均在九问iCode上使用GLM-5.2模型完成。测试质量由CodeBench评测，执行过程则记录Token消耗、耗时和工具调用情况，以便比较不同方案取得的结果及其所需开销。

通过三组对照，实验重点回答以下三个问题：

- **RQ1（主对照）：在当前模型上，完整Skill相比无Skill带来了多少质量收益和执行成本？**
- **RQ2（优化对比）：精简流程、整合执行脚本并补强约束后，质量与成本发生了什么变化？**
- **RQ3（自进化流程）：如何构建可复用于其他Skill的自进化流程？**

## RQ1：在当前模型上，完整Skill相比无Skill带来了多少质量收益和执行成本？

> **RA1：**在全部140个任务上，完整Skill相比无Skill平均评分高4.84分，Token开销和耗时分别约为后者的6.2倍和2.9倍。但在较简单的Apollo任务组中，无Skill已取得96.94分，完整Skill未带来评分提升。这说明部分简单任务依靠模型自身能力就能取得较好结果，是否保留完整流程，需要结合具体任务的收益与开销判断。

### 较简单的任务，无Skill也能取得高分

把任务组分开看，无Skill基线的作用会更清楚。较简单的Apollo任务组中，没有额外Skill指导，模型也取得了较高评分。

| 任务组 | 无Skill评分 | 旧版Skill评分 | 无Skill平均Token | 旧版Skill平均Token |
| --- | ---: | ---: | ---: | ---: |
| Apollo（较简单） | 96.94 | 96.70 | 74.7万 | 380.7万 |
| MyBatis-3（较复杂） | 89.73 | 95.29 | 61.1万 | 354.0万 |

Apollo任务中两种条件的分数很接近，0.24分的差异不能说明谁有显著优势。值得注意的是，无Skill已经能取得这一评分，加入完整流程后，Token增加到约5.1倍，却没有观察到评分提高。至少在这组任务上，让模型自行组织工作已经能够得到较好的结果。

MyBatis-3任务呈现了另一种情况：旧版Skill比无Skill高5.56分。这组较复杂的任务仍然能从Skill中受益。与Apollo的结果放在一起看，同一套流程对不同任务的帮助并不相同，处理简单任务时，未必需要执行整套流程。

**无Skill对照展示了模型在较简单任务上的能力，也提供了精简流程的依据。**看清不同任务的收益差异后，还需要比较Skill带来的质量提升和额外开销。

### 完整流程的整体收益与成本

| 条件 | 整体质量评分 | 平均每任务Token | 平均任务耗时 |
| --- | ---: | ---: | ---: |
| 无Skill | 88.84 | 62.3万 | 12.5分钟 |
| 完整Skill（旧版） | 93.68 | 387.6万 | 35.7分钟 |

合并全部140个任务后，无Skill的整体评分为88.84，完整Skill高4.84分。后者平均每任务多消耗约325.3万Token，多用约23.2分钟，Token和耗时分别约为基线的6.2倍和2.9倍。

是否值得为这4.84分付出这些成本，取决于任务的质量要求。不过，完整Skill的整体收益，并不意味着原有流程都需要保留。随着模型能力提升，部分步骤的作用可能已经减弱，执行它们的成本却依然存在。因此，还需要进一步验证：精简这些步骤后，能否以更低的开销取得相近的质量？

### 一份有用的Skill，也可能包含多余步骤

一条规则曾经有效，不代表它会一直有效。模型过去需要显式提醒才能完成的分析，现在可能已经会主动做。规则仍然留在Skill里，它要求的报告和检查也照常执行。

实际运行中的报告维护说明了这种成本怎样积累。一份分析报告写完，还要同步状态、更新评分，再由下一阶段读取。报告正文进入会话后，又可能作为历史输入出现在后续请求中。写出一次，后面还要处理多次。

这些步骤要分别看。报告如果补充了缺失信息，或者帮助下一阶段发现错误，就有保留理由；如果只是重新整理已经知道的结论，就可以尝试省去这一步，再检查是否影响后续任务。

完整Skill的平均评分更高，但在Apollo这类较简单的任务上，多出的流程没有带来更高的评分。后续改版减少了重复读取、报告维护等操作。省去这些工作后，开销能减少多少，测试质量会不会受到影响，需要用新旧版本的评测结果来判断。

## RQ2：精简流程、整合执行脚本并补强约束后，质量与成本发生了什么变化？

> **RA2：**新版相对旧版的累计会话Token下降67.9%，平均任务耗时下降40.2%，整体评分从93.68变为94.03。在本次评测中，新版用更少的Token和时间取得了与旧版相近的整体评分。

### 开销下降，整体评分相近

| 指标 | 完整Skill（旧版） | 优化Skill（新版） | 变化 |
| --- | ---: | ---: | ---: |
| 整体质量评分 | 93.68 | 94.03 | +0.35分 |
| 累计会话Token | 5.426亿 | 1.744亿 | −67.9% |
| 平均任务耗时 | 35.7分钟 | 21.3分钟 | −40.2% |

以无Skill组为参照，旧版平均每任务Token约为其6.2倍，新版降至约2倍，即124.5万。新版仍比无Skill组消耗更多Token，但与旧版相比，使用Skill的额外成本已经明显降低。

![三组质量与开销对比：新版相对旧版减少开销，整体评分相近，仍比无Skill基线消耗更多资源。](assets/02-quality-cost-comparison.png)

*图2：三组各包含140个对应任务。Token包含任务执行过程中各轮模型调用的输入与输出；耗时取单个任务执行时长的平均值。*



两版整体评分仅相差0.35分，不足以说明测试质量有显著提升。更明确的变化是，新版以更少的Token和时间完成了同一组任务，整体评分与旧版基本持平。

### 删去重复指导，将重复操作整合为脚本

这轮调整经历了六个阶段。最初先建立能完成任务的流程，使用中发现过程负担，随后精简说明、将重复操作整合为脚本，再从失败中补回约束，继续比较新旧表现。

![六阶段演进路径：能力构建、问题发现、流程精简、脚本整合、稳定性补强、评测迭代。](assets/03-evolution-path.png)

*图3：实际调整的先后顺序，各阶段没有单独测量收益。*

结合模型能力提升，大量通用流程说明和中间产物被删去，三个Skill之间的往返随之减少。精简说明后，模型仍需获取项目环境信息、执行编译和测试。如果每次都要重新查找执行入口、组织命令，就会在这些操作上花费不必要的时间和Token。

为减少这部分工作，新版Skill配套了两个脚本：`inspect`集中获取源码、测试代码和构建环境信息，`verify`统一执行编译和测试，返回验证结果。模型按需调用脚本，减少反复查找入口和组织命令的工作；未发生变化的项目证据则直接复用。Skill中的说明负责指导何时调用、如何使用结果，脚本负责执行具体操作。

| 过程指标 | 旧版 | 新版 |
| --- | ---: | ---: |
| 顶层工具调用次数 | 9,736 | 5,326 |
| 中间产物写入或修改次数 | 2,416 | 60 |
| Skill入口加载次数 | 362 | 419 |
| 每次入口加载的平均返回字符 | 22.7K | 4.9K |

表中的工具调用包括文件读写、命令执行等操作，不只统计这两个脚本的调用。

Skill入口加载次数增加了，平均返回内容却少了很多。一次调用返回的说明会进入上下文，并可能在后续轮次中被反复输入。缩短这部分内容，能够减少后续调用需要处理的重复信息。

### 根据运行反馈完善交付要求

流程精简后，部分任务出现了编译未通过或平台运行异常的情况。针对这些问题，新版补充了更明确的交付要求：代码需要能够运行，目标测试需要实际执行，验证结果也要与当前代码一致。这些要求用于检查任务是否完成，仍需保留在精简后的流程中。

模型已经掌握的分析方法，可以减少重复说明；当前代码能否通过测试，则需要实际运行才能确认。例如，修改前的测试通过记录，不能直接用于判断修改后的代码。因此，精简时需要区分通用方法的指导与当前结果的检查。前者可以随着模型能力提升而减少，后者关系到这次交付是否可靠。

在这轮迭代中，重复操作促使Skill删减流程，运行中发现的问题又促使它补充交付要求。修改的依据来自实际任务：哪些指导已无须反复提供，哪些检查仍然不能省去。运行反馈就这样进入了Skill的修改过程，也让自进化有了具体的内容。

## RQ3：如何构建可复用于其他Skill的自进化流程？

> **RA3：**其他Skill可以沿用明确交付标准、建立对照、修改规则、验证效果、更新版本的流程。人工确定任务要求和采用条件，模型辅助分析运行记录、修改Skill，再通过评测决定哪些改动进入新版本。模型升级或任务需求变化时，重新检查现有流程是否仍然适用。

这轮单元测试实验提供了一种可供其他Skill参考的迭代方法。迁移到新的任务时，需要重新定义交付标准，并配置相应的执行脚本；评估旧流程、根据反馈修改规则、验证后更新版本的做法则可以复用。

```text
明确任务要求与交付标准
          ↓
比较无Skill与现行Skill的表现
          ↓
结合运行记录，修改流程与配套脚本
          ↓
在相同条件下比较新旧版本的质量与开销
          ↓
采用有效改动，保存版本与修改理由
          ↓
模型升级或任务需求变化时，重新评估
```

### 先明确什么算做好，再判断哪些步骤有用

不同Skill需要不同的验收标准。例如，数据分析需要检查计算结果和统计口径，文档生成需要检查事实、内容完整性和格式要求。只有先明确这些要求，才能判断某个步骤被删去后，是否影响了任务结果。Token和耗时用于比较完成任务的成本，质量检查则决定这个结果能否接受。

建立对照时，可以同时观察无Skill与现行Skill的表现。对于模型已经能够稳定完成的简单任务，重点检查是否还有必要执行整套流程；对于仍需额外指导的任务，则保留能够改善结果的说明与约束。这样，同一份Skill可以根据任务需要采用不同的执行步骤。

模型升级后，这组对照也需要重新评估。原有步骤的必要性，应当根据当前模型的表现判断，不能只沿用旧版本模型上的结论。

### 让模型参与检查和修改自己的工作指南

复盘时，可以把Skill说明、任务要求、运行记录和评测结果一起交给模型，要求它指出哪些指令触发了重复工作，哪些要求没有被落实，并提出对应修改。每项建议都应说明依据和预期影响，方便人工检查，也便于比较修改前后的表现。

修改应当对应具体问题。已经能够自行完成的通用分析，可以减少步骤说明；反复执行且做法固定的操作，可以整理为脚本；只在特定条件下需要的检查，可以改成按需触发。涉及任务事实和交付验证的要求，则应保留明确的获取与检查方式。

这种做法把模型的分析能力用于Skill本身。模型既执行工作指南，也参与检查指南是否合适；人工结合任务要求审核修改，再交由评测检验效果。

### 用同一组任务检查改版效果

Skill改完以后，让新旧版本分别完成同一组任务，并保持模型和运行环境一致。比较时先看任务是否按要求完成，再看Token和耗时。如果只是少做了必要的检查，即使更快、更省，也不能算作有效改进。

还应加入修改过程中没有用到的任务，看看调整是否仍然有效。如果某项改动只适合简单任务，就在这类任务中使用，无须应用到所有场景。没有带来收益，或使结果变差的修改，可以不采用，继续使用旧版。

### 把有效改动写回Skill

确认修改有效后，就把新规则写进Skill，相关脚本也一并更新。只对某类任务适用的规则，要在说明中写清使用条件。同时保留旧版本，简要记录修改理由和评测结果，方便查阅，也便于在效果不如预期时恢复。

执行任务时，模型需要的是当前规则和任务材料。过去为什么增删某条规则，可以放在版本记录中，修改Skill时再查。这样既保留了经验，也不必让模型在每次任务中重新读一遍修改历史。

已经获取的信息可以在仍然有效时复用。输入材料、产物或运行环境发生变化后，只需重新检查受影响的部分。例如，输入数据更新了，原来的计算结果就需要重新验证。

![当前规则用于执行，任务信息按需获取，验证结果与当前条件对应，迭代记录用于复盘。](assets/04-information-lifecycle.png)

*图4：执行任务时读取当前规则与所需信息，修改Skill时再查阅迭代记录。*

这些版本记录也为下一次调整提供了起点。模型升级或任务要求变化后，可以重新检验已有规则，在保留有效经验的基础上继续修改。

## 结语：让Skill具备淘汰旧流程的能力

一套Skill凝结了工程经验，也记录了它诞生时模型的能力局限。随着后训练让模型掌握更多通用方法，过去有效的指导可能逐渐变成重复劳动。**允许旧步骤退出，应当成为Skill设计的一部分。**否则，模型能力提升以后，仍要为已经不再需要的指导付出执行成本。

这也改变了维护Skill时需要关注的问题。流程写得多完整、覆盖了多少经验，不足以说明它今天是否好用。更需要检查的是：当前模型还缺少什么帮助？通用分析方法可以逐渐交给模型，项目事实和交付要求则仍需明确，结果也要经过实际验证。随着模型能力变化，Skill应当持续调整指导的范围，让模型有更大的方法选择空间，同时保留对交付结果的检查。

这次实验中的自进化，体现在对Skill的具体修改中：根据运行记录减少重复说明和中间报告，将固定操作整理为脚本，再针对编译和运行中发现的问题补充交付要求。人工与模型协同完成这些调整，并通过新旧版本评测检查效果。其他Skill可以借鉴的，是根据运行反馈修改规则、用评测结果判断改动是否有效的做法。

模型持续进步，工程经验也需要接受重新检验。一条规则曾经解决过问题，是保留它的理由；模型和任务条件变化后，它是否仍然有效，则需要新的证据。**Skill的长期价值，在于让经验能够被复用，也能够被修正。**把这种更新能力纳入日常维护，积累下来的工作方法才能跟上模型的进步，持续服务于当前的任务。
