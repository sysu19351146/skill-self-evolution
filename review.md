# 全文与配图审查

审查日期：2026-09-08。已直接修改正文、四张PNG/SVG及其绘图脚本，并同步三份备用白板提示词。

## 总体判断

原稿的中心判断是：模型逐渐具备通用工作方法后，Skill应淘汰重复流程，并从运行反馈中补充必要约束。方向清楚，三组数据也能支撑重新评估旧版成本。分析较薄的部分在于，从操作记录跳到维护建议，中间缺少价值判断：一条规则解决什么损失、成本怎样累积、何时应条件触发、谁决定新版可以采用。

修订后的主线是：**每条规则都需要在当前模型、任务与环境下证明增量价值，并保留适用条件和退出条件。**实测结果作为案例，重点展开规则价值、执行成本和持续更新机制。

## 主要问题与修改

| 优先级 | 原稿问题 | 为什么需要改 | 已落实的修改 |
| --- | --- | --- | --- |
| 高 | 无Skill被主要解释为模型自身能力 | 对照仍使用平台、工具与运行环境，无法分离这些支持的贡献 | 说明无Skill基线代表基础执行环境下的表现 |
| 中 | 分组结果与维护策略之间衔接较少 | 任务组收益差异可以用于讨论支持的范围与深度 | 保留分组数据，补充将额外投入集中到更需要支持的任务上 |
| 中 | 会话Token的含义不够直观 | 读者需要区分会话处理规模和实际账单 | 保留缓存与累计输入输出的简要说明 |
| 中 | 主要展示整体降幅，未展开上升任务 | 具体任务可以提供下一轮问题定位的入口 | 加入5个Token上升、14个耗时上升任务，以及MyBatis-3案例 |
| 中 | 反复读取和维护报告容易被直接归为冗余 | 文件变化、上下文丢失、跨会话交接都可能需要这些操作 | 区分操作相似与功能冗余，将轨迹定位作为候选发现而非删除依据 |
| 中 | 脚本化只讲减少调用与命令组织 | 省下的工作可能转移为工具维护；接口也可能隐藏失败证据 | 补充固定操作复用、维护成本与可观察性，减少实现标识符 |
| 中 | RQ3接近一般的迭代步骤清单 | 缺少候选修改验收标准、过拟合已知任务和版本回退等关键机制 | 增加修改权与验收权分开维护、预先设门槛、未参与修改的任务和回退条件 |
| 中 | 自进化的自动化程度容易被标题放大 | 当前证据是人工与模型协同修改，没有完整自动候选筛选与发布系统 | 在实验背景和图4中明确当前实践与迁移建议的区别 |

## 新增分析如何依附证据

- **规则具有条件价值。**从两个任务组不同的质量增量出发，推导规则价值取决于模型、任务和环境，避免把更短当成普遍目标。
- **局部失败不应自动变成全局成本。**补充失败归因、条件触发及规则退出，解释Skill为何容易只增不减。
- **流程负担沿会话累积。**复核到节省的Token约98.3%来自输入侧，用于说明成本构成；明确不能归因为删除报告的独立收益。
- **脚本化转移工作与责任。**模型每次重新组织的稳定操作沉淀为可复用工具，同时产生维护与诊断责任。
- **从整体收益走向具体改进。**沿着开销增加的任务检查触发条件和额外工作，决定修改默认流程还是保留专门路径。
- **自进化受验收能力约束。**候选可以探索执行方法，成功标准需要独立维护；自动化程度应随可验证程度提高。
- **经验需要保存有效条件。**规则记录适用范围与退出条件，验证证据记录输入、产物、环境和检查范围，避免把历史通过记录无条件复用。

## 四张图逐项审查

| 配图 | 原图结论 | 修改与视觉核验 |
| --- | --- | --- |
| 图1 | 无明显遮挡；下方迭代步骤与图3重复 | 重画为通用指导、稳定操作、当前事实与证据三个职责区；下方对应收益、成本、适用范围 |
| 图2 | 三组数值、单位和从零起始的柱轴正确；可补充任务分布 | 保留数据和轴，分差使用中性灰；补报告值，以及上升/下降任务数，突出开销下降、整体评分相近 |
| 图3 | 六阶段顺序正确；没有实际反馈回路，第四到第五阶段的失败反馈箭头可能暗示脚本化导致失败 | 补第六阶段到第二阶段的虚线复评路径，将实线明确为调整顺序，注明失败来自整个运行过程；不分摊阶段收益 |
| 图4 | 原图只是四类信息卡片，能解释分类，不能解释更新如何被采用 | 重画为日常执行与方法更新六节点循环，单独维护的验收标准约束候选比较，补回退与证据适用条件 |

四图均已重建为2400×1350的PNG与对应SVG，并逐张视觉检查文字、框内留白、箭头路径和图注一致性。机制图沿用可编辑的Matplotlib源代码；备用手绘提示词另存，不替代当前实际配图。

## 数据复核与可追溯材料

本轮只读取相关原始材料，未修改原始实验目录。

- [原始无Skill记录](D:/wzh/gitcode/paper-writing-9-4/token_noskill.jsonl)、[旧版记录](D:/wzh/gitcode/paper-writing-9-4/token_metrics.jsonl)、[新版记录](D:/wzh/gitcode/paper-writing-9-4/token_newskill.jsonl)：复核三组140个唯一任务、7个项目、对应集合、模型字段、会话输入输出及耗时。
- [质量与方案比较报告](D:/wzh/gitcode/paper-writing-9-4/skill-comparison-report-with-files.html:172)：核对88.84、93.68、94.03整体评分；第220、221行核对Apollo与MyBatis-3评分。评分为报告值，未独立重跑质量评测。
- [过程比较报告](D:/wzh/gitcode/paper-writing-9-4/session-paired-analysis.html:22)：核对工具调用、中间产物写入、入口加载次数和返回字符数。这些过程指标沿用报告汇总，没有在本轮从完整会话轨迹重新统计。

三组Token总量分别为87,187,445、542,586,669、174,358,483；平均耗时分别为12.4776、35.6639、21.3352分钟。旧新Token降幅67.8653%，耗时降幅40.1771%。输入侧减少361,971,719Token，占总节省量98.3009%。

新增的案例来自配对记录：MyBatis-3任务mybatis-3/2从3,352,878Token增至6,637,642Token，增加约98.0%；正文使用接近两倍的描述。质量评分使用既有报告汇总。

绘图脚本原先把输入目录固定为项目上两级，在当前目录结构下无法正确刷新数据。已改为显式的--metrics-root参数；默认仍可只依靠随包快照重建配图。

## 外部机制核对

以下一手材料仅用于审查交叉核对，不用来补造本实验的缺失数据，也不加入正文的发布版参考列表：

- 从简单方案开始、通过评测决定何时增加工作流复杂度，与这里按增量收益判断流程的建议一致。[Anthropic：Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)
- Agent评测需要区分执行轨迹与最终结果，考虑多次运行的波动，并同时检查能力提升和已有能力退化。这些原则用于检查本文的论证边界。[Anthropic：Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)

## 当前复核范围

Token、耗时和上升/下降任务数已从原始记录重新汇总；质量评分和过程指标已与既有报告核对。正文以这些结果展开规则的增量价值、流程成本与经验更新机制。

